#include <stdbool.h>
#include <math.h>
#include <stdio.h>

bool isPalindrome(unsigned int n)
{
  char i = log(n) / log(10);
  unsigned int m = 1;

  while(i > 1)
  {
    m *= 10;
    i--;
  }
  printf("%d\n", m);

}

int main()
{
  printf("%d\n, isPalindrome(1001));
}
